
var Validus = Validus || require('../dist/validus.js').Validus;

suite('Validus Core Structure', function() {
    
  test('Should have a validators object');
  test('Should have a pipeline object');
  test('Should have a public validate function')
  test('Should have a public validateStep function')
  test('Should have a public validateBoolean function')
  
});